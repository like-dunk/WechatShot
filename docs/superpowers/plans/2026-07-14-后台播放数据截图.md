# 后台播放数据截图 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在现有"粉丝量截图"辅助图机制之外，新增第二种独立辅助图"后台播放数据截图"，两者可任意组合出现在 PPT 的 `release-info-screenshot`/`dawanqu` 两种模式中。

**Architecture:** 缓存与 UI 状态层按项目既有"每种辅助图一个平行模块"的风格新建（`playback-source-cache.js` 镜像 `fan-source-cache.js`，popup.js/auto-ppt.js 新增一套平行的 `currentPptPlaybackSource`/`resolvePlaybackImagesForPpt` 等）；pptx-builder.js 内部把 `序号_昵称` 匹配算法与排版分栏计算这两块逻辑通用化为 `Aux` 前缀函数，粉丝图与播放图共用同一份实现，避免维护两份几乎相同的高风险代码。

**Tech Stack:** 原生 JS（无构建步骤的 Chrome 扩展），IndexedDB，`tools/verify_extension_logic.js`（`vm.createContext` + `assert` 的自制回归测试脚本，无第三方测试框架）。

## Global Constraints

- 图片来源、适用范围、排版规则、代码组织方式均以设计文档为准，参见 `docs/superpowers/specs/2026-07-14-后台播放数据截图-design.md`。
- Excel/WPS 内嵌图片列表头模糊匹配关键词固定为「后台播放」。
- 仅 `release-info-screenshot`（发布信息截图单图单页）与 `dawanqu`（大湾区崭新模版）两种 PPT 模式支持后台播放数据截图；`clippings`/`link-screenshot` 不支持。
- 三图排版：主图占左半区；右半区在只有一个辅助图时铺满，两个辅助图都存在时上下二分（粉丝图在上、播放数据图在下）。
- 不修改任何 `.pptx` 模板二进制文件；自定义/大湾区模板的第 2 个占位图形被复用为"辅助图占位框总容器"。
- **本项目 `pptx-builder.js` 的 XML 生成函数（`buildReleaseInfoScreenshotSlideXml`/`buildReleaseInfoTemplateSlideXml`/`buildReleaseInfoScreenshotPptx`）依赖浏览器全局 `DOMParser`/`XMLSerializer`，`tools/verify_extension_logic.js` 的 Node `vm` 沙箱环境中没有这两个全局对象**（已用 `grep` 确认现有测试全部绕开了这一层，没有先例可循，引入 polyfill 依赖超出本次改动范围）。因此这一层的正确性靠 Task 14 的人工端到端验证覆盖，不在自动化测试范围内；其余不依赖 XML 的纯逻辑（匹配算法、排版分栏数学、结果字段）必须有自动化测试覆盖。
- 每个任务完成后代码库都应保持可运行（`node tools/verify_extension_logic.js` 全绿），任务之间不引入中间断裂状态。

---

### Task 1: 新建 playback-source-cache.js 缓存模块

**Files:**
- Create: `playback-source-cache.js`
- Modify: `tools/verify_extension_logic.js`（新增 `loadPlaybackSourceCacheExports` 与 `testPlaybackSourceCacheRoundTrip`）

**Interfaces:**
- Produces: `window.PlaybackSourceCache` / `self.PlaybackSourceCache`，API `putPlaybackSource(record): Promise<string>`、`getPlaybackSource(id): Promise<{id,name,files,createdAt}|undefined>`、`deletePlaybackSource(id): Promise<void>`、`cleanupOldPlaybackSources(maxAgeMs?): Promise<number>`。`record.files` 形如 `[{fileName, blob}]`。

- [ ] **Step 1: 写失败测试**

在 `tools/verify_extension_logic.js` 顶部函数区（紧邻 `loadTemplateCacheExports`，约第 203-211 行之后）新增：

```js
function loadPlaybackSourceCacheExports() {
  const context = vm.createContext(createBaseContext());
  context.window = context;
  context.indexedDB = createFakeIndexedDB();
  context.IDBKeyRange = { upperBound: (value) => ({ value }) };
  vm.runInContext(readSource("playback-source-cache.js"), context, { filename: "playback-source-cache.js" });
  return { PlaybackSourceCache: context.PlaybackSourceCache };
}
```

在 `testTemplateCacheRoundTrip` 函数之后（约第 837 行之后）新增：

```js
async function testPlaybackSourceCacheRoundTrip() {
  // 用内存版 IndexedDB 模拟，验证 PlaybackSourceCache 存取闭环：存入文件 -> 按 id 取回一致；
  // 删除后再取应返回 undefined。
  const { PlaybackSourceCache } = loadPlaybackSourceCacheExports();
  const files = [{ fileName: "1_抖音账号.png", blob: { arrayBuffer: async () => new Uint8Array([1, 2, 3]).buffer } }];
  const id = await PlaybackSourceCache.putPlaybackSource({ name: "后台播放数据截图", files });
  assert.ok(id && typeof id === "string" && id.startsWith("playback-"));
  const record = await PlaybackSourceCache.getPlaybackSource(id);
  assert.ok(record, "应能按 id 取回播放数据截图缓存记录");
  assert.strictEqual(record.name, "后台播放数据截图");
  assert.strictEqual(record.files.length, 1);
  assert.strictEqual(record.files[0].fileName, "1_抖音账号.png");
  const missing = await PlaybackSourceCache.getPlaybackSource("不存在的id");
  assert.strictEqual(missing, undefined);
  await PlaybackSourceCache.deletePlaybackSource(id);
  const afterDelete = await PlaybackSourceCache.getPlaybackSource(id);
  assert.strictEqual(afterDelete, undefined);
}
```

在文件末尾 `tests` 数组里，紧邻 `testTemplateCacheRoundTrip,`（约第 984 行）之后新增一行：

```js
  testPlaybackSourceCacheRoundTrip,
```

- [ ] **Step 2: 运行测试确认失败**

Run: `node tools/verify_extension_logic.js`
Expected: 抛出错误，提示找不到 `playback-source-cache.js` 文件（`ENOENT`），因为该文件还不存在。

- [ ] **Step 3: 创建 playback-source-cache.js**

```js
(() => {
  const DB_NAME = "shipinhao-playback-source-cache";
  const DB_VERSION = 1;
  const STORE_NAME = "playbackSources";
  const CREATED_INDEX = "createdAt";
  const DEFAULT_MAX_AGE_MS = 3 * 24 * 60 * 60 * 1000;

  function openDb() {
    return new Promise((resolve, reject) => {
      const request = indexedDB.open(DB_NAME, DB_VERSION);
      request.onupgradeneeded = () => {
        const db = request.result;
        const store = db.objectStoreNames.contains(STORE_NAME)
          ? request.transaction.objectStore(STORE_NAME)
          : db.createObjectStore(STORE_NAME, { keyPath: "id" });
        if (!store.indexNames.contains(CREATED_INDEX)) store.createIndex(CREATED_INDEX, "createdAt", { unique: false });
      };
      request.onsuccess = () => resolve(request.result);
      request.onerror = () => reject(request.error || new Error("打开后台播放数据截图缓存失败"));
    });
  }

  function requestToPromise(request) {
    return new Promise((resolve, reject) => {
      request.onsuccess = () => resolve(request.result);
      request.onerror = () => reject(request.error || new Error("后台播放数据截图缓存请求失败"));
    });
  }

  function buildPlaybackSourceId() {
    return `playback-${Date.now()}-${Math.random().toString(36).slice(2)}`;
  }

  async function putPlaybackSource(record) {
    if (!record || !Array.isArray(record.files) || !record.files.length) throw new Error("后台播放数据截图缓存记录不完整");
    const id = record.id || buildPlaybackSourceId();
    const stored = {
      id,
      name: record.name || "后台播放数据截图",
      files: record.files.map((file) => ({
        fileName: file.fileName || "截图.png",
        blob: file.blob,
      })),
      createdAt: record.createdAt || Date.now(),
    };
    const db = await openDb();
    try {
      const transaction = db.transaction(STORE_NAME, "readwrite");
      transaction.objectStore(STORE_NAME).put(stored);
      await new Promise((resolve, reject) => {
        transaction.oncomplete = () => resolve();
        transaction.onerror = () => reject(transaction.error || new Error("写入后台播放数据截图缓存失败"));
        transaction.onabort = () => reject(transaction.error || new Error("写入后台播放数据截图缓存中断"));
      });
    } finally {
      db.close();
    }
    return id;
  }

  async function getPlaybackSource(id) {
    if (!id) return null;
    const db = await openDb();
    try {
      const transaction = db.transaction(STORE_NAME, "readonly");
      return await requestToPromise(transaction.objectStore(STORE_NAME).get(id));
    } finally {
      db.close();
    }
  }

  async function deletePlaybackSource(id) {
    if (!id) return;
    const db = await openDb();
    try {
      const transaction = db.transaction(STORE_NAME, "readwrite");
      transaction.objectStore(STORE_NAME).delete(id);
      await new Promise((resolve, reject) => {
        transaction.oncomplete = () => resolve();
        transaction.onerror = () => reject(transaction.error || new Error("删除后台播放数据截图缓存失败"));
        transaction.onabort = () => reject(transaction.error || new Error("删除后台播放数据截图缓存中断"));
      });
    } finally {
      db.close();
    }
  }

  async function cleanupOldPlaybackSources(maxAgeMs = DEFAULT_MAX_AGE_MS) {
    const cutoff = Date.now() - maxAgeMs;
    const db = await openDb();
    try {
      return await new Promise((resolve, reject) => {
        let count = 0;
        const transaction = db.transaction(STORE_NAME, "readwrite");
        const index = transaction.objectStore(STORE_NAME).index(CREATED_INDEX);
        const request = index.openCursor(IDBKeyRange.upperBound(cutoff));
        request.onsuccess = () => {
          const cursor = request.result;
          if (!cursor) return;
          cursor.delete();
          count += 1;
          cursor.continue();
        };
        request.onerror = () => reject(request.error || new Error("读取旧后台播放数据截图缓存失败"));
        transaction.oncomplete = () => resolve(count);
        transaction.onerror = () => reject(transaction.error || new Error("清理旧后台播放数据截图缓存失败"));
        transaction.onabort = () => reject(transaction.error || new Error("清理旧后台播放数据截图缓存中断"));
      });
    } finally {
      db.close();
    }
  }

  const api = {
    putPlaybackSource,
    getPlaybackSource,
    deletePlaybackSource,
    cleanupOldPlaybackSources,
  };

  if (typeof self !== "undefined") self.PlaybackSourceCache = api;
  if (typeof window !== "undefined") window.PlaybackSourceCache = api;
})();
```

- [ ] **Step 4: 运行测试确认通过**

Run: `node tools/verify_extension_logic.js`
Expected: `✓ testPlaybackSourceCacheRoundTrip` 出现在输出中，且 `All N extension logic regression tests passed.`

- [ ] **Step 5: 提交**

```bash
git add playback-source-cache.js tools/verify_extension_logic.js
git commit -m "feat: add PlaybackSourceCache IndexedDB module for playback screenshot source"
```

---

### Task 2: pptx-builder.js 匹配算法通用化（Fan* → Aux*）

**Files:**
- Modify: `pptx-builder.js:622-702`（`normalizeFanSequence`/`getTaskFanSequence`/`findTaskForFanParsed`/`normalizeFanMatchKey`/`parseFanImageKey`/`buildFanImageMap`/`resolveFanImageFromImageName`/`resolveFanImageForTask`）
- Modify: `popup.js:831`（注释里的函数名引用）
- Modify: `tools/verify_extension_logic.js`（扩展 `loadPptxExportsWithInternals` 导出、新增 `testAuxImageMatching`）

**Interfaces:**
- Consumes: 无新依赖，纯重命名。
- Produces: `buildAuxImageMap(auxImages, tasks): Map`、`resolveAuxImageForTask(task, auxMap): image|null`、`resolveAuxImageFromImageName(image, auxMap): image|null`、`parseAuxImageKey(name): {sequence,nickname}|null`——供 Task 4 的 `buildReleaseInfoScreenshotItems` 调用。

- [ ] **Step 1: 写失败测试**

在 `tools/verify_extension_logic.js` 里把 `loadPptxExportsWithInternals`（约第 213-228 行）整体替换为：

```js
function loadPptxExportsWithInternals() {
  const context = vm.createContext(createBaseContext());
  context.window = context;
  let fetchImpl = async () => ({ ok: false, arrayBuffer: async () => new ArrayBuffer(0) });
  context.fetch = (url) => fetchImpl(url);
  context.chrome = { runtime: { getURL: (name) => `chrome-extension://test/${name}` } };
  const source = readSource("pptx-builder.js").replace(
    "  window.PptxClippings = {",
    [
      "  window.__loadTemplateFiles = loadTemplateFiles;",
      "  window.__buildReleaseInfoScreenshotItems = buildReleaseInfoScreenshotItems;",
      "  window.__buildAuxImageMap = buildAuxImageMap;",
      "  window.__resolveAuxImageForTask = resolveAuxImageForTask;",
      "  window.__resolveAuxImageFromImageName = resolveAuxImageFromImageName;",
      "  window.__splitBoxVertically = splitBoxVertically;",
      "  window.__layoutAuxImages = layoutAuxImages;",
      "  window.PptxClippings = {",
    ].join("\n")
  );
  vm.runInContext(source, context, { filename: "pptx-builder.js" });
  return {
    __loadTemplateFiles: context.__loadTemplateFiles,
    __buildReleaseInfoScreenshotItems: context.__buildReleaseInfoScreenshotItems,
    __buildAuxImageMap: context.__buildAuxImageMap,
    __resolveAuxImageForTask: context.__resolveAuxImageForTask,
    __resolveAuxImageFromImageName: context.__resolveAuxImageFromImageName,
    __splitBoxVertically: context.__splitBoxVertically,
    __layoutAuxImages: context.__layoutAuxImages,
    __setFetch: (impl) => { fetchImpl = impl; },
  };
}
```

在 `testPptMatching` 之后（约第 789 行之后）新增：

```js
function testAuxImageMatching() {
  const pptx = loadPptxExportsWithInternals();
  const tasks = [
    { importSequence: 1, sequence: "1", nickname: "抖音账号", fileName: "1_抖音账号.png" },
    { importSequence: 2, sequence: "9", nickname: "视频号账号", fileName: "2_视频号账号.png" },
  ];
  const auxImages = [{ name: "1_抖音账号.png" }, { name: "9_视频号账号.png" }];
  const map = pptx.__buildAuxImageMap(auxImages, tasks);
  // 精确匹配：文件名本身的序号_昵称能直接命中。
  assert.strictEqual(pptx.__resolveAuxImageForTask(tasks[0], map), auxImages[0]);
  // Excel 序号列（9）与插件权威 importSequence（2）不一致时，仍应按任务反查兜底命中。
  assert.strictEqual(pptx.__resolveAuxImageForTask(tasks[1], map), auxImages[1]);
  // 无匹配任务时返回 null，不应抛异常。
  const strangerTask = { importSequence: 3, nickname: "陌生账号" };
  assert.strictEqual(pptx.__resolveAuxImageForTask(strangerTask, map), null);
  // 按截图文件名兜底匹配（item.task 为空但截图文件名本身符合 序号_昵称）。
  assert.strictEqual(pptx.__resolveAuxImageFromImageName({ name: "1_抖音账号.png" }, map), auxImages[0]);
  assert.strictEqual(pptx.__resolveAuxImageFromImageName({ name: "未知.png" }, map), null);
}
```

在 `tests` 数组里紧邻 `testPptMatching,` 之后新增一行：

```js
  testAuxImageMatching,
```

- [ ] **Step 2: 运行测试确认失败**

Run: `node tools/verify_extension_logic.js`
Expected: 抛错 `pptx.__buildAuxImageMap is not a function`（因为 `pptx-builder.js` 里还是 `buildFanImageMap` 且未导出为 `__buildAuxImageMap`）。

- [ ] **Step 3: 重命名 pptx-builder.js 里的匹配算法函数**

把 `pptx-builder.js` 第 622-702 行整体替换为：

```js
  function normalizeAuxSequence(sequence) {
    const text = String(sequence == null ? "" : sequence).trim();
    if (/^\d+(\.0+)?$/.test(text)) return String(parseInt(text, 10));
    return text;
  }

  function getTaskAuxSequence(task) {
    if (task && task.importSequence != null && String(task.importSequence).trim() !== "") {
      return task.importSequence;
    }
    return task && task.sequence;
  }

  function findTaskForAuxParsed(parsed, tasks) {
    if (!parsed || !Array.isArray(tasks) || !tasks.length) return null;
    const targetSeq = normalizeAuxSequence(parsed.sequence);
    const targetNick = sanitizeFilenamePart(parsed.nickname, "").toLowerCase();
    return tasks.find((task) => {
      if (!task) return false;
      const nick = sanitizeFilenamePart(task.nickname, "").toLowerCase();
      if (nick !== targetNick) return false;
      const fileSeq = parseAuxImageKey(getBaseFileName(task.fileName));
      const candidates = [task.importSequence, task.sequence, fileSeq && fileSeq.sequence];
      return candidates.some((seq) => seq != null && String(seq).trim() !== "" && normalizeAuxSequence(seq) === targetSeq);
    }) || null;
  }

  function sanitizeFilenamePart(value) {
    let text = value == null ? "" : String(value).trim();
    // Windows 版 Chrome downloads API 禁止 ASCII ~，全角 ～（U+FF5E）视觉一致且跨平台可下载
    text = text.replace(/~/g, "～").replace(/[\\/:*?"<>|\r\n\t]+/g, "").replace(/\s+/g, "_").replace(/^[._ ]+|[._ ]+$/g, "");
    return text.slice(0, 80);
  }

  function normalizeAuxMatchKey(sequence, nickname) {
    const seq = normalizeAuxSequence(sequence);
    const nick = sanitizeFilenamePart(nickname, "").toLowerCase();
    return `${seq}_${nick}`;
  }

  function parseAuxImageKey(name) {
    const base = getBaseFileName(name);
    const index = base.indexOf("_");
    if (index < 0) return null;
    return {
      sequence: base.slice(0, index).trim(),
      nickname: base.slice(index + 1).trim(),
    };
  }

  // 通用「序号_昵称」辅助图匹配算法，粉丝量截图与后台播放数据截图共用同一份实现。
  function buildAuxImageMap(auxImages, tasks = []) {
    const map = new Map();
    const availableTasks = Array.isArray(tasks) ? tasks : [];
    (auxImages || []).forEach((image) => {
      const parsed = parseAuxImageKey(image && image.name);
      if (!parsed) return;
      const fileKey = normalizeAuxMatchKey(parsed.sequence, parsed.nickname);
      if (fileKey && !map.has(fileKey)) map.set(fileKey, image);
      // 与 Excel 内嵌路径一致：手动上传的「序号_昵称」若能对应到某条任务，
      // 再按该任务权威的 importSequence+昵称 注册一份，避免 Excel 序号列与插件连续编号不一致时对不上。
      const task = findTaskForAuxParsed(parsed, availableTasks);
      if (task) {
        const taskKey = normalizeAuxMatchKey(getTaskAuxSequence(task), task.nickname);
        if (taskKey && !map.has(taskKey)) map.set(taskKey, image);
      }
    });
    return map;
  }

  function resolveAuxImageFromImageName(image, auxMap) {
    if (!image || !auxMap || !auxMap.size) return null;
    const parsed = parseAuxImageKey(image.name);
    if (!parsed) return null;
    return auxMap.get(normalizeAuxMatchKey(parsed.sequence, parsed.nickname)) || null;
  }

  function resolveAuxImageForTask(task, auxMap) {
    if (!task || !auxMap || !auxMap.size) return null;
    const key = normalizeAuxMatchKey(getTaskAuxSequence(task), task.nickname);
    return auxMap.get(key) || null;
  }
```

同时把 `popup.js:831` 的注释更新为引用新函数名（原文引用了 `pptx-builder.js 的 parseFanImageKey`）：

```js
      // 去重后缀必须加在序号段而非昵称段：pptx-builder.js 的 parseAuxImageKey 按第一个下划线
```

- [ ] **Step 4: 运行测试确认通过**

Run: `node tools/verify_extension_logic.js`
Expected: `✓ testAuxImageMatching` 出现，全部测试通过（`testPptMatching`/其余现有测试不受影响，因为这几个函数当前无外部调用方——Task 4 才会把 `buildReleaseInfoScreenshotItems` 接回新函数名）。

> 注意：此时 `buildReleaseInfoScreenshotItems`（第 599-620 行）内部仍在调用旧名 `buildFanImageMap`/`resolveFanImageForTask`/`resolveFanImageFromImageName`，本步会导致这几处引用出错（`ReferenceError`）。因为 `loadPptxExports()`（供 `testPptMatching`/`testOutputFolderNaming` 等使用）只是把整个文件跑一遍 `vm.runInContext`，函数体内的引用错误只有在真正调用到 `buildReleaseInfoScreenshotItems` 时才会抛出——而现有测试都不会触发它，所以本步测试仍会全绿。为避免遗留死引用，紧接着立刻执行 Step 5 里的最小修复，把 `buildReleaseInfoScreenshotItems` 里的三处旧函数名同步改为新名（不引入 playbackImage，那是 Task 4 的范围）：

```js
  function buildReleaseInfoScreenshotItems(images, tasks, options = {}) {
    const title = normalizeLinkScreenshotText(options.title) || RELEASE_INFO_DEFAULT_TITLE;
    const availableTasks = Array.isArray(tasks) ? tasks : [];
    const fanMap = buildAuxImageMap(options.fanImages || [], availableTasks);
    return buildLinkScreenshotItems(images, availableTasks).map((item) => {
      const task = item.task || {};
      const releaseInfo = task.releaseInfo || {};
      const fanImage = item.task
        ? resolveAuxImageForTask(item.task, fanMap) || resolveAuxImageFromImageName(item.image, fanMap)
        : null;
      return {
        image: item.image,
        fanImage,
        title,
        account: normalizeLinkScreenshotText(releaseInfo.account) || normalizeLinkScreenshotText(task.nickname) || item.nickname,
        platform: normalizeLinkScreenshotText(releaseInfo.platform) || normalizeLinkScreenshotText(task.platformLabel),
        publishTitle: normalizeLinkScreenshotText(releaseInfo.title),
        link: normalizeLinkScreenshotText(releaseInfo.link) || normalizeLinkScreenshotText(task.url) || item.linkText,
        time: normalizeLinkScreenshotText(releaseInfo.time),
      };
    });
  }
```

再次运行 `node tools/verify_extension_logic.js` 确认仍全绿（`testPptMatching` 覆盖的是 `buildLinkScreenshotItems`，不经过本函数，因此这一步修复不会影响它）。

- [ ] **Step 5: 提交**

```bash
git add pptx-builder.js popup.js tools/verify_extension_logic.js
git commit -m "refactor: generalize fan-image matching helpers into Aux* functions"
```

---

### Task 3: pptx-builder.js 新增排版分栏通用函数

**Files:**
- Modify: `pptx-builder.js`（在 `splitBoxHorizontally` 之后，约第 995-1001 行之后新增 `splitBoxVertically`/`layoutAuxImages`）
- Modify: `tools/verify_extension_logic.js`（新增 `testLayoutAuxImages`）

**Interfaces:**
- Consumes: `fitPictureIntoBox(image, box)`（已存在，第 1031 行）。
- Produces: `splitBoxVertically(box): [box, box]`、`layoutAuxImages(auxImages, auxBox): [{image, position}]`——供 Task 5 的 `buildReleaseInfoScreenshotSlideXml`/`buildReleaseInfoTemplateSlideXml` 使用。`auxImages` 必须是调用方已按 `[fan, playback].filter(Boolean)` 过滤好、固定顺序（粉丝在前）的数组。

- [ ] **Step 1: 写失败测试**

在 `tools/verify_extension_logic.js` 的 `testAuxImageMatching` 之后新增：

```js
function testLayoutAuxImages() {
  const pptx = loadPptxExportsWithInternals();
  const box = { x: 100, y: 200, cx: 1000, cy: 800 };
  const [top, bottom] = pptx.__splitBoxVertically(box);
  assert.deepStrictEqual(top, { x: 100, y: 200, cx: 1000, cy: 400 });
  assert.deepStrictEqual(bottom, { x: 100, y: 600, cx: 1000, cy: 400 });
  // 单张辅助图：铺满整个 auxBox（等价于原有「仅粉丝图」时的效果）。
  const single = pptx.__layoutAuxImages([{ width: 16, height: 9 }], box);
  assert.strictEqual(single.length, 1);
  assert.ok(single[0].position.cy <= box.cy);
  // 两张辅助图：按传入顺序上下二分，第一张（粉丝图）在上半区，第二张（播放图）在下半区。
  const fan = { width: 16, height: 9 };
  const playback = { width: 16, height: 9 };
  const both = pptx.__layoutAuxImages([fan, playback], box);
  assert.strictEqual(both.length, 2);
  assert.strictEqual(both[0].image, fan);
  assert.strictEqual(both[1].image, playback);
  assert.ok(both[0].position.y < both[1].position.y, "粉丝图应排在播放数据图上方");
}
```

在 `tests` 数组里紧邻 `testAuxImageMatching,` 之后新增一行：

```js
  testLayoutAuxImages,
```

- [ ] **Step 2: 运行测试确认失败**

Run: `node tools/verify_extension_logic.js`
Expected: 抛错 `pptx.__splitBoxVertically is not a function`。

- [ ] **Step 3: 实现 splitBoxVertically / layoutAuxImages**

在 `pptx-builder.js` 的 `splitBoxHorizontally` 函数（第 995-1001 行）之后插入：

```js
  function splitBoxVertically(box) {
    const halfCy = Math.round(box.cy / 2);
    return [
      { x: box.x, y: box.y, cx: box.cx, cy: halfCy },
      { x: box.x, y: box.y + halfCy, cx: box.cx, cy: box.cy - halfCy },
    ];
  }

  // auxImages 固定顺序 [粉丝图, 播放图]（由调用方过滤 null 后传入）：单张辅助图铺满整个
  // auxBox（与改动前"仅粉丝图"时的效果一致）；两张辅助图时对 auxBox 上下二分，粉丝图在上、
  // 播放数据图在下。
  function layoutAuxImages(auxImages, auxBox) {
    if (auxImages.length <= 1) {
      return auxImages.map((image) => ({ image, position: fitPictureIntoBox(image, auxBox) }));
    }
    const [topBox, bottomBox] = splitBoxVertically(auxBox);
    return [
      { image: auxImages[0], position: fitPictureIntoBox(auxImages[0], topBox) },
      { image: auxImages[1], position: fitPictureIntoBox(auxImages[1], bottomBox) },
    ];
  }
```

- [ ] **Step 4: 运行测试确认通过**

Run: `node tools/verify_extension_logic.js`
Expected: `✓ testLayoutAuxImages` 出现，全部测试通过。

- [ ] **Step 5: 提交**

```bash
git add pptx-builder.js tools/verify_extension_logic.js
git commit -m "feat: add splitBoxVertically/layoutAuxImages for stacking two aux images"
```

---

### Task 4: buildReleaseInfoScreenshotItems 支持 playbackImage 匹配

**Files:**
- Modify: `pptx-builder.js:599-620`（`buildReleaseInfoScreenshotItems`，Task 2 Step 5 已把内部函数名改为 Aux* 但尚未接入 `playbackImages`）
- Modify: `tools/verify_extension_logic.js`（新增 `testReleaseInfoItemsResolveFanAndPlayback`）

**Interfaces:**
- Consumes: `buildAuxImageMap`/`resolveAuxImageForTask`/`resolveAuxImageFromImageName`（Task 2 产出）。
- Produces: `buildReleaseInfoScreenshotItems(images, tasks, options)` 返回的每个 item 新增 `playbackImage` 字段（与既有 `fanImage` 并列，各自独立 resolve，互不影响）。`options.playbackImages` 是新的可选入参。

- [ ] **Step 1: 写失败测试**

在 `tools/verify_extension_logic.js` 的 `testLayoutAuxImages` 之后新增：

```js
function testReleaseInfoItemsResolveFanAndPlayback() {
  const pptx = loadPptxExportsWithInternals();
  const images = [
    { name: "1_抖音账号.png" },
    { name: "2_视频号账号.png" },
    { name: "3_头条账号.png" },
  ];
  const tasks = [
    { fileName: "1_抖音账号.png", sequence: "1", importSequence: 1, nickname: "抖音账号", url: "https://www.douyin.com/video/1" },
    { fileName: "2_视频号账号.png", sequence: "2", importSequence: 2, nickname: "视频号账号", url: "https://weixin.qq.com/sph/a" },
    { fileName: "3_头条账号.png", sequence: "3", importSequence: 3, nickname: "头条账号", url: "https://www.toutiao.com/article/1" },
  ];
  // 账号1只有粉丝图，账号2只有播放图，账号3两者都没有——覆盖三种组合，
  // 确保「仅播放图、无粉丝图」不会被误判成粉丝图（对应设计文档 4.2 节的修正点）。
  const fanImages = [{ name: "1_抖音账号.png" }];
  const playbackImages = [{ name: "2_视频号账号.png" }];
  const items = pptx.__buildReleaseInfoScreenshotItems(images, tasks, { fanImages, playbackImages });
  const byAccount = new Map(items.map((item) => [item.account, item]));
  assert.ok(byAccount.get("抖音账号").fanImage, "账号1应匹配到粉丝图");
  assert.strictEqual(byAccount.get("抖音账号").playbackImage, null, "账号1不应匹配到播放图");
  assert.strictEqual(byAccount.get("视频号账号").fanImage, null, "账号2不应匹配到粉丝图");
  assert.ok(byAccount.get("视频号账号").playbackImage, "账号2应匹配到播放图");
  assert.strictEqual(byAccount.get("头条账号").fanImage, null);
  assert.strictEqual(byAccount.get("头条账号").playbackImage, null);
}
```

在 `tests` 数组里紧邻 `testLayoutAuxImages,` 之后新增一行：

```js
  testReleaseInfoItemsResolveFanAndPlayback,
```

- [ ] **Step 2: 运行测试确认失败**

Run: `node tools/verify_extension_logic.js`
Expected: `账号2应匹配到播放图` 断言失败（`playbackImage` 当前始终为 `undefined`，因为 `buildReleaseInfoScreenshotItems` 还没有读取 `options.playbackImages`）。

- [ ] **Step 3: 实现 playbackImage 匹配**

把 `pptx-builder.js` 里的 `buildReleaseInfoScreenshotItems`（Task 2 Step 5 改过的版本）整体替换为：

```js
  function buildReleaseInfoScreenshotItems(images, tasks, options = {}) {
    const title = normalizeLinkScreenshotText(options.title) || RELEASE_INFO_DEFAULT_TITLE;
    const availableTasks = Array.isArray(tasks) ? tasks : [];
    const fanMap = buildAuxImageMap(options.fanImages || [], availableTasks);
    const playbackMap = buildAuxImageMap(options.playbackImages || [], availableTasks);
    return buildLinkScreenshotItems(images, availableTasks).map((item) => {
      const task = item.task || {};
      const releaseInfo = task.releaseInfo || {};
      const fanImage = item.task
        ? resolveAuxImageForTask(item.task, fanMap) || resolveAuxImageFromImageName(item.image, fanMap)
        : null;
      const playbackImage = item.task
        ? resolveAuxImageForTask(item.task, playbackMap) || resolveAuxImageFromImageName(item.image, playbackMap)
        : null;
      return {
        image: item.image,
        fanImage,
        playbackImage,
        title,
        account: normalizeLinkScreenshotText(releaseInfo.account) || normalizeLinkScreenshotText(task.nickname) || item.nickname,
        platform: normalizeLinkScreenshotText(releaseInfo.platform) || normalizeLinkScreenshotText(task.platformLabel),
        publishTitle: normalizeLinkScreenshotText(releaseInfo.title),
        link: normalizeLinkScreenshotText(releaseInfo.link) || normalizeLinkScreenshotText(task.url) || item.linkText,
        time: normalizeLinkScreenshotText(releaseInfo.time),
      };
    });
  }
```

- [ ] **Step 4: 运行测试确认通过**

Run: `node tools/verify_extension_logic.js`
Expected: `✓ testReleaseInfoItemsResolveFanAndPlayback` 出现，全部测试通过。

- [ ] **Step 5: 提交**

```bash
git add pptx-builder.js tools/verify_extension_logic.js
git commit -m "feat: resolve playbackImage independently from fanImage per screenshot item"
```

---

### Task 5: buildReleaseInfoScreenshotPptx / SlideXml 支持三图排版（关键正确性修复）

**Files:**
- Modify: `pptx-builder.js:782-993`（`buildReleaseInfoScreenshotPptx`、`buildReleaseInfoScreenshotSlideXml`、`buildReleaseInfoTemplateSlideXml`）

**Interfaces:**
- Consumes: `item.fanImage`/`item.playbackImage`（Task 4 产出）、`layoutAuxImages`/`splitBoxVertically`（Task 3 产出）。
- Produces: `buildReleaseInfoScreenshotPptx` 内部的 `imageLinks` 从**位置数组**改为**带 `main`/`fan`/`playback` 键名的对象**——这是本次改动里唯一的正确性修复点：原代码用 `imageLinks[1]` 隐式代表"粉丝图"，一旦辅助图可能是"仅播放、无粉丝"，继续用位置下标会把播放图误渲染成粉丝图占位。本任务无自动化测试覆盖（`DOMParser`/`XMLSerializer` 在 Node 测试沙箱里不可用，见 Global Constraints），必须对照下方代码逐行核对，并在 Task 14 做人工端到端验证。

- [ ] **Step 1: 替换 buildReleaseInfoScreenshotPptx**

把 `pptx-builder.js` 第 782-850 行整体替换为：

```js
  function buildReleaseInfoScreenshotPptx(files, items, options = {}) {
    const preserveTemplateLayout = Boolean(options.preserveTemplateLayout);
    const presentationDoc = parseXml(decodeText(files.get("ppt/presentation.xml")));
    const presentationRelsDoc = parseXml(decodeText(files.get("ppt/_rels/presentation.xml.rels")));
    const contentTypesDoc = parseXml(decodeText(files.get("[Content_Types].xml")));
    const slides = getPresentationSlides(presentationDoc, presentationRelsDoc, files);
    if (!slides.length) throw new Error("发布信息截图单图单页模板中没有幻灯片");
    const templateIndex = preserveTemplateLayout
      ? findReleaseInfoTemplateSlideIndex(slides, files)
      : Math.min(RELEASE_INFO_TEMPLATE_INDEX, slides.length - 1);
    const templateSlide = slides[templateIndex];
    const baseSlideXml = decodeText(files.get(`ppt/${templateSlide.target}`));
    const baseSlideFileName = templateSlide.target.split("/").pop();
    const baseRelsPath = `ppt/slides/_rels/${baseSlideFileName}.rels`;
    const baseRelsXml = files.has(baseRelsPath) ? decodeText(files.get(baseRelsPath)) : emptyRelationshipsXml();
    let nextSlideNumber = getNextSlideNumber(files);
    let nextSlideId = getMaxSlideId(presentationDoc) + 1;
    let nextPresentationRelId = getNextRelNumber(presentationRelsDoc);
    let nextMediaNumber = getNextMediaNumber(files);
    let slideInsertAnchor = templateSlide.node;
    items.forEach((item, index) => {
      const isFirst = index === 0;
      const slideTarget = isFirst ? templateSlide.target : `slides/slide${nextSlideNumber}.xml`;
      const slideFileName = slideTarget.split("/").pop();
      const relsPath = `ppt/slides/_rels/${slideFileName}.rels`;
      const registerMedia = (image) => {
        const mediaName = `image${nextMediaNumber}.${image.ext}`;
        nextMediaNumber += 1;
        const link = { ...image, relId: "", target: `../media/${mediaName}` };
        files.set(`ppt/media/${mediaName}`, image.data);
        return link;
      };
      // imageLinks 用 kind 显式区分 main/fan/playback，不能再用数组下标隐式代表某一种辅助图：
      // 辅助图现在可能是「仅粉丝」「仅播放」「两者都有」三种情况之一，位置下标 1 在
      // 「仅播放、无粉丝」时会指向播放图而非粉丝图，若渲染层继续假定下标 1 恒为粉丝图，
      // 会把播放图错误地渲染成粉丝图占位（标签、位置都会用错）。
      const imageLinks = { main: registerMedia(item.image) };
      if (item.fanImage) imageLinks.fan = registerMedia(item.fanImage);
      if (item.playbackImage) imageLinks.playback = registerMedia(item.playbackImage);
      const slideRels = buildReleaseInfoSlideRelationships(baseRelsXml, Object.values(imageLinks));
      const slideXml = preserveTemplateLayout
        ? buildReleaseInfoTemplateSlideXml(baseSlideXml, imageLinks, item)
        : buildReleaseInfoScreenshotSlideXml(baseSlideXml, imageLinks, item);
      files.set(`ppt/${slideTarget}`, encodeText(slideXml));
      files.set(relsPath, encodeText(slideRels));
      ensureSlideOverride(contentTypesDoc, `/ppt/${slideTarget}`);
      if (!isFirst) {
        const relId = `rId${nextPresentationRelId}`;
        nextPresentationRelId += 1;
        addPresentationRelationship(presentationRelsDoc, relId, slideTarget);
        slideInsertAnchor = addPresentationSlideId(presentationDoc, slideInsertAnchor, nextSlideId, relId);
        nextSlideId += 1;
        nextSlideNumber += 1;
      }
    });
    ensureImageContentTypes(contentTypesDoc);
    files.set("ppt/presentation.xml", encodeText(serializeXml(presentationDoc)));
    files.set("ppt/_rels/presentation.xml.rels", encodeText(serializeXml(presentationRelsDoc)));
    files.set("[Content_Types].xml", encodeText(serializeXml(contentTypesDoc)));
    updateAppSlideCount(files, slides.length + items.length - 1);
    return createZip(Array.from(files.entries()).map(([name, data]) => ({ name, data })));
  }
```

（`buildReleaseInfoSlideRelationships` 第 852-868 行本就按数组遍历注册 relId，`Object.values(imageLinks)` 直接兼容，无需改动。）

- [ ] **Step 2: 替换 buildReleaseInfoScreenshotSlideXml（内置模板路径）**

把原第 923-949 行整体替换为：

```js
  function buildReleaseInfoScreenshotSlideXml(baseSlideXml, imageLinks, item) {
    const doc = parseXml(baseSlideXml);
    Array.from(doc.getElementsByTagNameNS(PML, "sp")).forEach((shape) => shape.parentNode.removeChild(shape));
    Array.from(doc.getElementsByTagNameNS(PML, "pic")).forEach((picture) => picture.parentNode.removeChild(picture));
    const spTree = doc.getElementsByTagNameNS(PML, "spTree")[0];
    let nextShapeId = getMaxShapeId(doc) + 1;
    spTree.appendChild(createTextBoxNode(doc, nextShapeId, "PPT 大标题", item.title, RELEASE_INFO_LAYOUT.title, { fontSize: 20, bold: true, borderColor: "e15252" }));
    nextShapeId += 1;
    buildReleaseInfoRows(item).forEach((row, index) => {
      spTree.appendChild(createTextBoxNode(doc, nextShapeId, `发布信息 ${index + 1}`, row.text, row.position, { fontSize: row.fontSize }));
      nextShapeId += 1;
    });
    const mainImage = imageLinks.main;
    const auxImages = [imageLinks.fan, imageLinks.playback].filter(Boolean);
    if (auxImages.length) {
      const [leftBox, rightBox] = splitBoxHorizontally(RELEASE_INFO_LAYOUT.image);
      spTree.appendChild(createPictureNode(doc, nextShapeId, item.account || "发布截图", mainImage.relId, fitPictureIntoBox(mainImage, leftBox)));
      nextShapeId += 1;
      layoutAuxImages(auxImages, rightBox).forEach(({ image, position }) => {
        const label = image === imageLinks.fan ? "粉丝量截图" : "后台播放数据截图";
        spTree.appendChild(createPictureNode(doc, nextShapeId, label, image.relId, position));
        nextShapeId += 1;
      });
    } else {
      const pic = createPictureNode(doc, nextShapeId, item.account || "发布截图", mainImage.relId, buildReleaseInfoPicturePosition(mainImage));
      spTree.appendChild(pic);
    }
    return serializeXml(doc);
  }
```

- [ ] **Step 3: 替换 buildReleaseInfoTemplateSlideXml（自定义/大湾区模板路径）**

把原第 960-993 行整体替换为：

```js
  // Custom-template path: keep the uploaded template's top title box and non-screenshot
  // images (e.g. logo). Middle slot labels are removed and rebuilt with the built-in
  // RELEASE_INFO_LAYOUT rows; screenshot placeholders are swapped for real images.
  function buildReleaseInfoTemplateSlideXml(baseSlideXml, imageLinks, item) {
    const doc = parseXml(baseSlideXml);
    const shapes = Array.from(doc.getElementsByTagNameNS(PML, "sp"));
    const titleShape = findReleaseInfoTitleShape(shapes);
    removeReleaseInfoMiddleShapes(doc, titleShape);
    const pictures = Array.from(doc.getElementsByTagNameNS(PML, "pic"));
    const mainPlaceholder = pictures[0] || null;
    const fanPlaceholder = pictures[1] || null;
    const mainImage = imageLinks.main;
    const auxImages = [imageLinks.fan, imageLinks.playback].filter(Boolean);
    const spTree = doc.getElementsByTagNameNS(PML, "spTree")[0];
    let nextShapeId = getMaxShapeId(doc) + 1;
    buildReleaseInfoRows(item).forEach((row, index) => {
      spTree.appendChild(createTextBoxNode(doc, nextShapeId, `发布信息 ${index + 1}`, row.text, row.position, { fontSize: row.fontSize }));
      nextShapeId += 1;
    });
    // 只移除我们实际要替换的主图/辅助图占位图形，模板里其余图片（如 logo）保持不动。
    [mainPlaceholder, fanPlaceholder].forEach((picture) => { if (picture) picture.parentNode.removeChild(picture); });
    const mainBox = mainPlaceholder ? getShapeGeometry(mainPlaceholder) : buildReleaseInfoPicturePosition(mainImage);
    if (!auxImages.length) {
      spTree.appendChild(createPictureNode(doc, getMaxShapeId(doc) + 1, item.account || "发布截图", mainImage.relId, fitPictureIntoBox(mainImage, mainBox)));
    } else {
      // 模板只保留了 1 个辅助图占位（fanPlaceholder）：占位存在时把它自身几何框当作辅助区，
      // 主图铺满 mainBox 不收缩；占位不存在时对 mainBox 做左右二分。辅助区内若同时有
      // 粉丝图与播放图，再由 layoutAuxImages 对辅助区上下二分——不需要模板里新增第 3 个占位。
      const [mainRenderBox, auxBox] = fanPlaceholder
        ? [mainBox, getShapeGeometry(fanPlaceholder)]
        : splitBoxHorizontally(mainBox);
      spTree.appendChild(createPictureNode(doc, getMaxShapeId(doc) + 1, item.account || "发布截图", mainImage.relId, fitPictureIntoBox(mainImage, mainRenderBox)));
      layoutAuxImages(auxImages, auxBox).forEach(({ image, position }) => {
        const label = image === imageLinks.fan ? "粉丝量截图" : "后台播放数据截图";
        spTree.appendChild(createPictureNode(doc, getMaxShapeId(doc) + 1, label, image.relId, position));
      });
    }
    return serializeXml(doc);
  }
```

- [ ] **Step 4: 逐条核对四种辅助图组合（人工代码走查，非自动化测试）**

对照下表在编辑器里逐行确认新代码的分支命中符合预期（`DOMParser` 在测试沙箱不可用，这一步用阅读代码代替运行测试，Task 14 会用真实浏览器环境跑一遍验证）：

| 组合 | 内置模板路径 | 自定义/大湾区模板路径（fanPlaceholder 存在） | 自定义/大湾区模板路径（fanPlaceholder 缺失） |
|---|---|---|---|
| 无粉丝无播放 | 主图铺满 `RELEASE_INFO_LAYOUT.image` 整框 | 主图铺满 `mainBox`（`mainPlaceholder` 几何框） | 主图铺满 `mainBox` |
| 仅粉丝 | 左右对半分，粉丝铺满右半区 | 主图铺满 `mainBox`，粉丝铺满 `fanPlaceholder` 几何框 | 左右对半分，粉丝铺满右半区 |
| 仅播放 | 左右对半分，播放铺满右半区，标签为"后台播放数据截图" | 主图铺满 `mainBox`，播放铺满 `fanPlaceholder` 几何框，标签为"后台播放数据截图" | 左右对半分，播放铺满右半区 |
| 粉丝+播放 | 左右对半分，右半区再上下二分（粉丝上、播放下） | 主图铺满 `mainBox`，`fanPlaceholder` 几何框再上下二分（粉丝上、播放下） | 左右对半分，右半区再上下二分 |

- [ ] **Step 5: 运行现有回归测试确认无回归**

Run: `node tools/verify_extension_logic.js`
Expected: 全部测试仍然通过（本任务未改动任何被测函数的签名或行为，`buildReleaseInfoScreenshotItems`/`buildAuxImageMap` 等 Task 2-4 测试不受影响）。

- [ ] **Step 6: 提交**

```bash
git add pptx-builder.js
git commit -m "fix: render aux images by kind (main/fan/playback) instead of array index"
```

---

### Task 6: 结果统计字段 + loadFanImagesFromCacheRecord 重命名

**Files:**
- Modify: `pptx-builder.js`（`buildReleaseInfoScreenshotFromImages`/`buildDawanquFromImages` 结果对象；`loadFanImagesFromCacheRecord` → `loadImagesFromCacheRecord`；导出列表）
- Modify: `popup.js:967`
- Modify: `auto-ppt.js:52`

**Interfaces:**
- Produces: `window.PptxClippings.loadImagesFromCacheRecord(record)`（原 `loadFanImagesFromCacheRecord`，函数体不变，仅改名——它本就是与"fan"无关的通用 blob→image 转换）；`buildReleaseInfoScreenshotFromImages`/`buildDawanquFromImages` 的返回对象新增 `playbackMatchedCount: number`。

- [ ] **Step 1: 重命名 loadFanImagesFromCacheRecord**

把 `pptx-builder.js` 第 711-719 行的函数定义：

```js
  async function loadFanImagesFromCacheRecord(record) {
```

改为：

```js
  async function loadImagesFromCacheRecord(record) {
```

（函数体其余部分不变。）

把导出列表（约第 1587 行）里的：

```js
    loadFanImagesFromCacheRecord,
```

改为：

```js
    loadImagesFromCacheRecord,
```

- [ ] **Step 2: 同步更新两处调用方**

`popup.js:967`，把：

```js
      if (record) return await window.PptxClippings.loadFanImagesFromCacheRecord(record);
```

改为：

```js
      if (record) return await window.PptxClippings.loadImagesFromCacheRecord(record);
```

`auto-ppt.js:52`，把：

```js
    if (record) return await window.PptxClippings.loadFanImagesFromCacheRecord(record);
```

改为：

```js
    if (record) return await window.PptxClippings.loadImagesFromCacheRecord(record);
```

- [ ] **Step 3: 新增 playbackMatchedCount 统计字段**

`pptx-builder.js` 里 `buildReleaseInfoScreenshotFromImages` 的返回对象（紧邻现有 `fanMatchedCount: items.filter((item) => item.fanImage).length,`）追加一行：

```js
      fanMatchedCount: items.filter((item) => item.fanImage).length,
      playbackMatchedCount: items.filter((item) => item.playbackImage).length,
```

`buildDawanquFromImages` 的返回对象做同样追加：

```js
      fanMatchedCount: items.filter((item) => item.fanImage).length,
      playbackMatchedCount: items.filter((item) => item.playbackImage).length,
```

- [ ] **Step 4: 运行测试确认无回归**

Run: `node tools/verify_extension_logic.js`
Expected: 全部测试通过（`loadImagesFromCacheRecord` 未被任何现有测试直接调用，纯改名不影响断言）。

- [ ] **Step 5: 提交**

```bash
git add pptx-builder.js popup.js auto-ppt.js
git commit -m "feat: add playbackMatchedCount and rename loadFanImagesFromCacheRecord to loadImagesFromCacheRecord"
```

---

### Task 7: background.js 透传 autoPptPlaybackSourceId

**Files:**
- Modify: `background.js:1523-1546`（`normalizeOptions`）
- Modify: `background.js:1737-1758`（`saveAutoPptSessionFromState`）
- Modify: `tools/verify_extension_logic.js`（新增 `testAutoPptPlaybackSourceIdFlowsThroughOptions`）

**Interfaces:**
- Produces: `state.options.autoPptPlaybackSourceId`（字符串，随 `START` 消息透传、随 auto-ppt session 落盘）。

- [ ] **Step 1: 写失败测试**

在 `tools/verify_extension_logic.js` 的 `testAutoPptTemplateIdFlowsThroughOptions` 之后（约第 818 行之后）新增：

```js
function testAutoPptPlaybackSourceIdFlowsThroughOptions() {
  const background = loadBackgroundExports();
  // 后台播放数据截图缓存 id 必须从启动 options 一路保留到 normalizeOptions 输出，
  // 逻辑与 autoPptFanSourceId/autoPptTemplateId 完全对称。
  const withPlayback = background.normalizeOptions(
    { autoGeneratePpt: true, autoPptMode: "release-info-screenshot", autoPptPlaybackSourceId: "playback-abc-123" },
    [{ url: "https://weixin.qq.com/sph/a", platform: "weixin" }]
  );
  assert.strictEqual(withPlayback.autoPptPlaybackSourceId, "playback-abc-123");
  assert.strictEqual(background.getPublicOptions(withPlayback).autoPptPlaybackSourceId, "playback-abc-123");
  // 没有缓存后台播放数据截图时，字段为空字符串。
  const noPlayback = background.normalizeOptions(
    { autoGeneratePpt: true, autoPptMode: "clippings" },
    [{ url: "https://weixin.qq.com/sph/a", platform: "weixin" }]
  );
  assert.strictEqual(noPlayback.autoPptPlaybackSourceId, "");
}
```

在 `tests` 数组里紧邻 `testAutoPptTemplateIdFlowsThroughOptions,` 之后新增一行：

```js
  testAutoPptPlaybackSourceIdFlowsThroughOptions,
```

- [ ] **Step 2: 运行测试确认失败**

Run: `node tools/verify_extension_logic.js`
Expected: `assert.strictEqual(withPlayback.autoPptPlaybackSourceId, "playback-abc-123")` 失败，实际值为 `undefined`。

- [ ] **Step 3: 实现透传**

`background.js` 的 `normalizeOptions` 函数（第 1523-1546 行）里，紧邻 `autoPptFanSourceId: String(options.autoPptFanSourceId || ""),`（第 1542 行）之后新增一行：

```js
    autoPptFanSourceId: String(options.autoPptFanSourceId || ""),
    autoPptPlaybackSourceId: String(options.autoPptPlaybackSourceId || ""),
```

`saveAutoPptSessionFromState` 函数（第 1737-1758 行）里，紧邻 `autoPptFanSourceId: state.options.autoPptFanSourceId || "",`（第 1747 行）之后新增一行：

```js
      autoPptFanSourceId: state.options.autoPptFanSourceId || "",
      autoPptPlaybackSourceId: state.options.autoPptPlaybackSourceId || "",
```

- [ ] **Step 4: 运行测试确认通过**

Run: `node tools/verify_extension_logic.js`
Expected: `✓ testAutoPptPlaybackSourceIdFlowsThroughOptions` 出现，全部测试通过。

- [ ] **Step 5: 提交**

```bash
git add background.js tools/verify_extension_logic.js
git commit -m "feat: pass autoPptPlaybackSourceId through normalizeOptions and auto-ppt session"
```

---

### Task 8: popup.html 新增后台播放数据截图上传 UI

**Files:**
- Modify: `popup.html:228-241`（紧邻 `#pptFanSourcePicker`/`#pptFanSourceInfo` 之后）
- Modify: `popup.html`（脚本引入区，紧邻 `<script src="fan-source-cache.js"></script>`）

**Interfaces:**
- Produces: DOM 元素 `#pptPlaybackSourcePicker`、`#pptPlaybackZipInput`、`#pptPlaybackFolderInput`、`#pptPlaybackSourceInfo`——供 Task 10/11 的 `elements` 对象读取。

- [ ] **Step 1: 新增上传 UI 区块**

在 `popup.html` 第 241 行（`<div id="pptFanSourceInfo" ...>...</div>`）之后插入：

```html
      <div id="pptPlaybackSourcePicker" class="ppt-source-picker" hidden>
        <div class="ppt-source-title">选择后台播放数据截图来源（可选）</div>
        <div class="ppt-source-actions">
          <label class="ppt-source-action">
            <input id="pptPlaybackZipInput" type="file" accept=".zip,application/zip,application/x-zip-compressed">
            <span>选择 ZIP</span>
          </label>
          <label class="ppt-source-action">
            <input id="pptPlaybackFolderInput" type="file" webkitdirectory multiple>
            <span>选择文件夹</span>
          </label>
        </div>
      </div>
      <div id="pptPlaybackSourceInfo" class="muted" hidden>后台播放数据截图按 Excel 序号+昵称命名（如 1_车源凯.png）；勾选自动 PPT 时需在开始截图前选好。</div>
```

- [ ] **Step 2: 引入新缓存脚本**

在 `popup.html` 底部，把：

```html
  <script src="screenshot-cache.js"></script>
  <script src="fan-source-cache.js"></script>
  <script src="pptx-builder.js"></script>
  <script src="popup.js"></script>
```

改为：

```html
  <script src="screenshot-cache.js"></script>
  <script src="fan-source-cache.js"></script>
  <script src="playback-source-cache.js"></script>
  <script src="pptx-builder.js"></script>
  <script src="popup.js"></script>
```

- [ ] **Step 3: 浏览器手动验证 HTML 有效**

Run: 在 Chrome 里打开 `chrome://extensions`，若已加载本插件则点"重新加载"；否则"加载已解压的扩展程序"选择本项目目录，再点插件图标打开弹窗。
Expected: 弹窗正常打开、无控制台报错（此时 `popup.js` 里还没有 `elements.pptPlaybackSourcePicker` 等引用，Task 11 完成前新 UI 不会显示，属预期状态）。

- [ ] **Step 4: 提交**

```bash
git add popup.html
git commit -m "feat: add playback screenshot source picker UI to popup"
```

---

### Task 9: auto-ppt.html 引入新缓存脚本

**Files:**
- Modify: `auto-ppt.html`

**Interfaces:**
- Produces: `window.PlaybackSourceCache` 在 `auto-ppt.html` 页面内可用，供 Task 13 使用。

- [ ] **Step 1: 引入脚本**

把 `auto-ppt.html` 底部：

```html
  <script src="screenshot-cache.js"></script>
  <script src="fan-source-cache.js"></script>
  <script src="pptx-builder.js"></script>
  <script src="auto-ppt.js"></script>
```

改为：

```html
  <script src="screenshot-cache.js"></script>
  <script src="fan-source-cache.js"></script>
  <script src="playback-source-cache.js"></script>
  <script src="pptx-builder.js"></script>
  <script src="auto-ppt.js"></script>
```

- [ ] **Step 2: 提交**

```bash
git add auto-ppt.html
git commit -m "chore: load playback-source-cache.js in auto-ppt.html"
```

---

### Task 10: popup.js Excel 内嵌兜底提取

**Files:**
- Modify: `popup.js`（状态变量、`extractPlaybackImagesFromCellImages`/`resolvePlaybackColumnIndex`、`handleTaskFiles`/`applyImportedTaskFiles`/`resetImportState` 接入）

**Interfaces:**
- Consumes: `findLikelyHeaderRowIndex`/`buildTasks`/`normalizeHeader`/`getMaxColumnCount`（均已存在）、`window.PptxClippings.normalizeImage`。
- Produces: 全局变量 `currentPptPlaybackSource`/`currentExcelPlaybackImages`/`pptPlaybackSourceReadToken`；函数 `extractPlaybackImagesFromCellImages(rows, cellImages, fileName)`；`importedTaskFiles` 每项新增 `playbackImages` 字段。

- [ ] **Step 1: 新增状态变量**

在 `popup.js` 第 92 行（`let currentExcelFanImages = [];`）之后插入：

```js
let currentPptPlaybackSource = null;
// 未手动上传后台播放数据截图时的兜底来源：从已导入 Excel 里模糊匹配到「后台播放」的列中提取内嵌截图，
// 已按「序号_昵称.ext」命名，可直接当作 playbackImages 使用。手动上传的 currentPptPlaybackSource 始终优先。
let currentExcelPlaybackImages = [];
```

在第 96 行（`let pptFanSourceReadToken = 0;`）之后插入：

```js
let pptPlaybackSourceReadToken = 0;
```

- [ ] **Step 2: 新增 Excel 内嵌提取函数**

在 `resolveFanColumnIndex` 函数（第 850-863 行）之后插入：

```js
// 从某个已解析 Excel 文件的 cellImages 中，挑出实际内嵌了截图、且表头模糊匹配「后台播放」的
// 那一列，产出与手动上传文件夹同名规则一致的 playbackImages 条目。逻辑与
// extractFanImagesFromCellImages 完全对称，仅表头关键词与产物字段名不同。
async function extractPlaybackImagesFromCellImages(rows, cellImages, fileName = "") {
  if (!Array.isArray(cellImages) || !cellImages.length || !Array.isArray(rows)) return [];
  const headerRowIndex = findLikelyHeaderRowIndex(rows);
  const headerRow = rows[headerRowIndex] || [];
  const playbackColumnIndex = resolvePlaybackColumnIndex(headerRow, cellImages);
  if (playbackColumnIndex < 0) return [];
  const sequenceMode = elements.sequenceModeInput ? elements.sequenceModeInput.value : "sequence";
  const { tasks: rowTasks } = buildTasks(rows, null, sequenceMode);
  const taskByRow = new Map();
  rowTasks.forEach((task) => {
    if (!taskByRow.has(task.rowNumber - 1)) taskByRow.set(task.rowNumber - 1, task);
  });
  const usedNames = new Set();
  const images = [];
  for (const cellImage of cellImages) {
    if (cellImage.col !== playbackColumnIndex || cellImage.row <= headerRowIndex) continue;
    const task = taskByRow.get(cellImage.row);
    if (!task || !task.nickname) continue;
    const baseName = `${task.importSequence}_${task.nickname}`;
    let name = `${baseName}.${cellImage.ext}`;
    let counter = 2;
    while (usedNames.has(name)) {
      name = `${task.importSequence}-${counter}_${task.nickname}.${cellImage.ext}`;
      counter += 1;
    }
    usedNames.add(name);
    try {
      images.push(await window.PptxClippings.normalizeImage(name, cellImage.bytes));
    } catch (error) {
      addLog(`解析「${fileName}」内嵌后台播放数据截图失败（${task.nickname}）：${error.message}`, "warning");
    }
  }
  return images;
}

// 表头模糊匹配「后台播放」的列可能不止一个，此时优先选真正承载内嵌图片的那一列。
function resolvePlaybackColumnIndex(headerRow, cellImages) {
  const candidateCols = [];
  for (let col = 0; col < getMaxColumnCount([headerRow]); col += 1) {
    if (normalizeHeader(headerRow[col]).includes("后台播放")) candidateCols.push(col);
  }
  if (!candidateCols.length) return -1;
  if (candidateCols.length === 1) return candidateCols[0];
  const imageColCounts = new Map();
  cellImages.forEach((image) => {
    if (candidateCols.includes(image.col)) imageColCounts.set(image.col, (imageColCounts.get(image.col) || 0) + 1);
  });
  if (!imageColCounts.size) return candidateCols[0];
  return Array.from(imageColCounts.entries()).sort((a, b) => b[1] - a[1])[0][0];
}
```

- [ ] **Step 3: 接入 handleTaskFiles / applyImportedTaskFiles / resetImportState**

`handleTaskFiles`（第 387-418 行）里，把：

```js
    const fanImages = await extractFanImagesFromCellImages(parsed.rows, parsed.cellImages, file.name);
    incoming.push({ fileName: file.name, rows: parsed.rows, fillMarks: parsed.fillMarks, fanImages });
```

改为：

```js
    const fanImages = await extractFanImagesFromCellImages(parsed.rows, parsed.cellImages, file.name);
    const playbackImages = await extractPlaybackImagesFromCellImages(parsed.rows, parsed.cellImages, file.name);
    incoming.push({ fileName: file.name, rows: parsed.rows, fillMarks: parsed.fillMarks, fanImages, playbackImages });
```

`applyImportedTaskFiles`（第 420-469 行）里，把：

```js
    currentExcelFanImages = importedTaskFiles.flatMap((file) => file.fanImages || []);
    if (currentExcelFanImages.length) {
      addLog(`已从 Excel 内嵌图片自动识别到 ${currentExcelFanImages.length} 张粉丝量截图，未手动上传时将按 Excel 序号+昵称自动匹配`, "success");
    }
    syncPptFanSourceInfo();
```

改为：

```js
    currentExcelFanImages = importedTaskFiles.flatMap((file) => file.fanImages || []);
    if (currentExcelFanImages.length) {
      addLog(`已从 Excel 内嵌图片自动识别到 ${currentExcelFanImages.length} 张粉丝量截图，未手动上传时将按 Excel 序号+昵称自动匹配`, "success");
    }
    syncPptFanSourceInfo();
    currentExcelPlaybackImages = importedTaskFiles.flatMap((file) => file.playbackImages || []);
    if (currentExcelPlaybackImages.length) {
      addLog(`已从 Excel 内嵌图片自动识别到 ${currentExcelPlaybackImages.length} 张后台播放数据截图，未手动上传时将按 Excel 序号+昵称自动匹配`, "success");
    }
    syncPptPlaybackSourceInfo();
```

`resetImportState`（第 496-507 行）里，把：

```js
  currentExcelFanImages = [];
  syncPptFanSourceInfo();
  elements.startButton.disabled = true;
```

改为：

```js
  currentExcelFanImages = [];
  syncPptFanSourceInfo();
  currentExcelPlaybackImages = [];
  syncPptPlaybackSourceInfo();
  elements.startButton.disabled = true;
```

> 本步引用了 Task 11 才会新增的 `syncPptPlaybackSourceInfo` 函数——这是本计划里唯一一处"先接后建"的顺序依赖，因为 Excel 提取逻辑和手动上传 UI 逻辑在原代码里本就写在同一批函数附近。完成 Step 3 后先不要单独跑手动验证，直接进入 Task 11；Task 11 完成后 `syncPptPlaybackSourceInfo` 才存在，两个任务合起来才是可运行状态。

- [ ] **Step 4: 提交**

```bash
git add popup.js
git commit -m "feat: extract playback screenshots from embedded Excel cell images"
```

---

### Task 11: popup.js 手动上传 UI 与显隐控制

**Files:**
- Modify: `popup.js`（`elements` 对象、`init()` 事件绑定、`setupUploadDropZones`、新增 `handlePptPlaybackZipChange/File`/`handlePptPlaybackFolderChange/Files`/`syncPptPlaybackSourceInfo`、`syncPptModeUi`、`handlePptModeChange`、`generatePpt` 禁用状态、`applyState` 禁用状态）

**Interfaces:**
- Consumes: Task 8 新增的 DOM 元素、Task 10 新增的 `currentPptPlaybackSource`/`currentExcelPlaybackImages`/`pptPlaybackSourceReadToken`。
- Produces: `syncPptPlaybackSourceInfo()`（Task 10 Step 3 依赖）。

- [ ] **Step 1: elements 对象新增引用**

在 `popup.js` 第 138 行（`pptFanSourceInfo: document.getElementById("pptFanSourceInfo"),`）之后插入：

```js
  pptPlaybackSourcePicker: document.getElementById("pptPlaybackSourcePicker"),
  pptPlaybackZipInput: document.getElementById("pptPlaybackZipInput"),
  pptPlaybackFolderInput: document.getElementById("pptPlaybackFolderInput"),
  pptPlaybackSourceInfo: document.getElementById("pptPlaybackSourceInfo"),
```

- [ ] **Step 2: init() 事件绑定**

在第 198 行（`elements.pptFanFolderInput.addEventListener("change", handlePptFanFolderChange);`）之后插入：

```js
  elements.pptPlaybackZipInput.addEventListener("change", handlePptPlaybackZipChange);
  elements.pptPlaybackFolderInput.addEventListener("change", handlePptPlaybackFolderChange);
```

- [ ] **Step 3: setupUploadDropZones 拖拽上传**

在 `setupUploadDropZones`（第 235-275 行）里，紧邻 `pptFanFolderInput` 的 `if` 块（第 263-269 行）之后插入：

```js
  if (elements.pptPlaybackZipInput) {
    setupUploadDropZone(elements.pptPlaybackZipInput.closest(".ppt-source-action"), {
      input: elements.pptPlaybackZipInput,
      multiple: false,
      onDrop: (files) => handlePptPlaybackZipFile(files[0]),
    });
  }
  if (elements.pptPlaybackFolderInput) {
    setupUploadDropZone(elements.pptPlaybackFolderInput.closest(".ppt-source-action"), {
      input: elements.pptPlaybackFolderInput,
      directory: true,
      onDrop: handlePptPlaybackFolderFiles,
    });
  }
```

- [ ] **Step 4: 新增上传处理函数**

在 `syncPptFanSourceInfo` 函数（第 787-798 行）之后插入：

```js
async function handlePptPlaybackZipChange(event) {
  const file = event.target.files && event.target.files[0];
  await handlePptPlaybackZipFile(file);
}

async function handlePptPlaybackZipFile(file) {
  if (isGeneratingPpt) return;
  const readToken = ++pptPlaybackSourceReadToken;
  currentPptPlaybackSource = null;
  if (!file) {
    syncPptPlaybackSourceInfo();
    return;
  }
  try {
    if (!/\.zip$/i.test(file.name)) throw new Error("请选择 .zip 压缩包");
    elements.pptPlaybackSourceInfo.textContent = `正在读取后台播放数据截图：${file.name}`;
    addLog(`开始读取后台播放数据截图压缩包：${file.name}`);
    const analysis = await window.PptxClippings.inspectZipFile(file);
    if (readToken !== pptPlaybackSourceReadToken) return;
    if (!analysis.imageCount) throw new Error("压缩包中没有找到 png、jpg、jpeg 或 webp 图片");
    currentPptPlaybackSource = { type: "zip", name: file.name, file, imageCount: analysis.imageCount };
    if (elements.pptPlaybackFolderInput) elements.pptPlaybackFolderInput.value = "";
    syncPptPlaybackSourceInfo();
    addLog(`后台播放数据截图压缩包读取完成：${analysis.imageCount} 张图片`, "success");
  } catch (error) {
    if (readToken !== pptPlaybackSourceReadToken) return;
    currentPptPlaybackSource = null;
    elements.pptPlaybackSourceInfo.textContent = `读取失败：${error.message}`;
    addLog(`后台播放数据截图压缩包读取失败：${error.message}`, "failed");
  }
}

async function handlePptPlaybackFolderChange(event) {
  await handlePptPlaybackFolderFiles(Array.from(event.target.files || []));
}

async function handlePptPlaybackFolderFiles(files) {
  if (isGeneratingPpt) return;
  const readToken = ++pptPlaybackSourceReadToken;
  currentPptPlaybackSource = null;
  if (!files.length) {
    syncPptPlaybackSourceInfo();
    return;
  }
  const folderName = getPptFolderName(files);
  try {
    elements.pptPlaybackSourceInfo.textContent = `正在读取后台播放数据截图：${folderName}`;
    addLog(`开始读取后台播放数据截图文件夹：${folderName}`);
    const analysis = await window.PptxClippings.inspectImageFiles(files);
    if (readToken !== pptPlaybackSourceReadToken) return;
    if (!analysis.imageCount) throw new Error("文件夹中没有找到 png、jpg、jpeg 或 webp 图片");
    currentPptPlaybackSource = { type: "folder", name: folderName, files, imageCount: analysis.imageCount };
    if (elements.pptPlaybackZipInput) elements.pptPlaybackZipInput.value = "";
    syncPptPlaybackSourceInfo();
    addLog(`后台播放数据截图文件夹读取完成：${analysis.imageCount} 张图片`, "success");
  } catch (error) {
    if (readToken !== pptPlaybackSourceReadToken) return;
    currentPptPlaybackSource = null;
    elements.pptPlaybackSourceInfo.textContent = `读取失败：${error.message}`;
    addLog(`后台播放数据截图文件夹读取失败：${error.message}`, "failed");
  }
}

function syncPptPlaybackSourceInfo() {
  if (!elements.pptPlaybackSourceInfo || !isReleaseInfoLikeMode(elements.pptModeInput.value)) return;
  if (currentPptPlaybackSource) {
    elements.pptPlaybackSourceInfo.textContent = `${currentPptPlaybackSource.name}，识别到 ${currentPptPlaybackSource.imageCount} 张后台播放数据截图，将按 Excel 序号+昵称精确匹配`;
    return;
  }
  if (currentExcelPlaybackImages.length) {
    elements.pptPlaybackSourceInfo.textContent = `未手动上传，已从 Excel 内嵌图片自动识别到 ${currentExcelPlaybackImages.length} 张后台播放数据截图，将按 Excel 序号+昵称精确匹配（手动上传可覆盖）`;
    return;
  }
  elements.pptPlaybackSourceInfo.textContent = "后台播放数据截图按 Excel 序号+昵称命名（如 1_车源凯.png）；勾选自动 PPT 时需在开始截图前选好。若 Excel 表格里已内嵌后台播放数据截图（列名含“后台播放”），无需上传即可自动识别。";
}
```

- [ ] **Step 5: syncPptModeUi 与 handlePptModeChange 接入显隐控制**

把 `syncPptModeUi`（第 551-580 行）整体替换为：

```js
function syncPptModeUi() {
  const isReleaseInfoMode = elements.pptModeInput.value === "release-info-screenshot";
  const isCustomTemplate = elements.pptTemplateSourceInput.value === "custom" && Boolean(currentPptTemplate);
  const showFanSource = isReleaseInfoLikeMode(elements.pptModeInput.value);
  if (elements.pptFanSourcePicker) {
    elements.pptFanSourcePicker.hidden = !showFanSource;
    elements.pptFanSourcePicker.style.display = showFanSource ? "" : "none";
  }
  if (elements.pptFanSourceInfo) {
    elements.pptFanSourceInfo.hidden = !showFanSource;
    elements.pptFanSourceInfo.style.display = showFanSource ? "" : "none";
  }
  if (elements.pptPlaybackSourcePicker) {
    elements.pptPlaybackSourcePicker.hidden = !showFanSource;
    elements.pptPlaybackSourcePicker.style.display = showFanSource ? "" : "none";
  }
  if (elements.pptPlaybackSourceInfo) {
    elements.pptPlaybackSourceInfo.hidden = !showFanSource;
    elements.pptPlaybackSourceInfo.style.display = showFanSource ? "" : "none";
  }
  if (!showFanSource) {
    currentPptFanSource = null;
    // 让此刻仍在进行中的 ZIP/文件夹异步读取失效，避免它稍后 resolve 时把刚清空的
    // currentPptFanSource 又悄悄写回去（handlePptFanZipFile/handlePptFanFolderFiles
    // 的过期检查依赖这个 token 递增）。
    pptFanSourceReadToken += 1;
    if (elements.pptFanZipInput) elements.pptFanZipInput.value = "";
    if (elements.pptFanFolderInput) elements.pptFanFolderInput.value = "";
    currentPptPlaybackSource = null;
    pptPlaybackSourceReadToken += 1;
    if (elements.pptPlaybackZipInput) elements.pptPlaybackZipInput.value = "";
    if (elements.pptPlaybackFolderInput) elements.pptPlaybackFolderInput.value = "";
  }
  // With a custom template the title box is preserved from the template itself,
  // so the editable title field only applies to built-in templates.
  const showTitleField = isReleaseInfoMode && !isCustomTemplate;
  if (elements.pptReleaseTitleField) {
    elements.pptReleaseTitleField.hidden = !showTitleField;
    elements.pptReleaseTitleField.style.display = showTitleField ? "" : "none";
  }
  updateRefloEnrichmentInfo();
}
```

把 `handlePptModeChange`（第 541-549 行）整体替换为：

```js
function handlePptModeChange() {
  syncPptModeUi();
  syncPptFanSourceInfo();
  syncPptPlaybackSourceInfo();
  if (currentPptSource) {
    elements.pptZipInfo.textContent = buildPptSourceInfo(currentPptSource.name, currentPptSource.imageCount);
    return;
  }
  elements.pptZipInfo.textContent = getCurrentPptMode().info;
}
```

- [ ] **Step 6: generatePpt 与 applyState 里的禁用状态**

在 `generatePpt`（第 865-904 行）里，紧邻第 874-875 行：

```js
    if (elements.pptFanZipInput) elements.pptFanZipInput.disabled = true;
    if (elements.pptFanFolderInput) elements.pptFanFolderInput.disabled = true;
```

之后插入：

```js
    if (elements.pptPlaybackZipInput) elements.pptPlaybackZipInput.disabled = true;
    if (elements.pptPlaybackFolderInput) elements.pptPlaybackFolderInput.disabled = true;
```

紧邻第 895-896 行（`finally` 块内）：

```js
    if (elements.pptFanZipInput) elements.pptFanZipInput.disabled = Boolean(running);
    if (elements.pptFanFolderInput) elements.pptFanFolderInput.disabled = Boolean(running);
```

之后插入：

```js
    if (elements.pptPlaybackZipInput) elements.pptPlaybackZipInput.disabled = Boolean(running);
    if (elements.pptPlaybackFolderInput) elements.pptPlaybackFolderInput.disabled = Boolean(running);
```

在 `applyState` 内（第 3428-3429 行）：

```js
  if (elements.pptFanZipInput) elements.pptFanZipInput.disabled = running || pptBusy;
  if (elements.pptFanFolderInput) elements.pptFanFolderInput.disabled = running || pptBusy;
```

之后插入：

```js
  if (elements.pptPlaybackZipInput) elements.pptPlaybackZipInput.disabled = running || pptBusy;
  if (elements.pptPlaybackFolderInput) elements.pptPlaybackFolderInput.disabled = running || pptBusy;
```

- [ ] **Step 7: 浏览器手动验证**

Run: 重新加载插件，打开弹窗，把"PPT 模式"切到"发布信息截图单图单页"。
Expected: 页面上同时出现"选择粉丝量截图来源（可选）"和"选择后台播放数据截图来源（可选）"两个上传区块；切到"发布剪报多图铺页"或"链接截图单图单页"时两者都应隐藏；控制台无报错。

- [ ] **Step 8: 提交**

```bash
git add popup.js
git commit -m "feat: wire playback screenshot manual upload UI into popup"
```

---

### Task 12: popup.js 解析/持久化/生成流程接入 playbackImages

**Files:**
- Modify: `popup.js`（新增 `resolvePlaybackImagesForPpt`/`persistPlaybackSourceForAutoPpt`/`shouldPersistPlaybackSourceForAutoPpt`；`buildPptByMode`/`buildPptResultInfo`/`buildPptSourceInfo`/`startRun`/`autoGeneratePptFromCompletedRun` 接入）

**Interfaces:**
- Consumes: `window.PlaybackSourceCache`（Task 1）、`window.PptxClippings.loadImagesFromCacheRecord`（Task 6）、`currentPptPlaybackSource`/`currentExcelPlaybackImages`（Task 10）。
- Produces: `options.playbackImages`/`options.autoPptPlaybackSourceId` 贯穿 `buildPptByMode` → `PptxClippings.buildReleaseInfoScreenshotFromImageFiles`/`buildDawanquFromImageFiles` 等入口。

- [ ] **Step 1: 新增 resolvePlaybackImagesForPpt / persistPlaybackSourceForAutoPpt / shouldPersistPlaybackSourceForAutoPpt**

在 `shouldPersistFanSourceForAutoPpt` 函数（第 1029-1035 行）之后插入：

```js
async function resolvePlaybackImagesForPpt(options = {}) {
  if (Array.isArray(options.playbackImages)) return options.playbackImages;
  if (options.autoPptPlaybackSourceId && window.PlaybackSourceCache) {
    try {
      const record = await window.PlaybackSourceCache.getPlaybackSource(options.autoPptPlaybackSourceId);
      if (record) return await window.PptxClippings.loadImagesFromCacheRecord(record);
    } catch (error) {
      addLog(`读取后台播放数据截图缓存失败，尝试改用当前弹窗内仍保留的来源：${error.message}`, "warning");
    }
  }
  if (!isReleaseInfoLikeMode(options.modeValue || elements.pptModeInput.value)) return [];
  const playbackSource = options.playbackSource || currentPptPlaybackSource;
  if (playbackSource) {
    try {
      return await window.PptxClippings.loadImagesFromPptSource(playbackSource);
    } catch (error) {
      addLog(`读取后台播放数据截图来源失败：${error.message}`, "warning");
      return [];
    }
  }
  return currentExcelPlaybackImages;
}

async function persistPlaybackSourceForAutoPpt() {
  if (!window.PlaybackSourceCache) return "";
  await window.PlaybackSourceCache.cleanupOldPlaybackSources();
  const files = [];
  if (currentPptPlaybackSource && currentPptPlaybackSource.type === "zip") {
    const images = await window.PptxClippings.loadImagesFromPptSource(currentPptPlaybackSource);
    images.forEach((image) => {
      files.push({
        fileName: getBaseFileName(image.name),
        blob: new Blob([image.data], { type: image.mime || "image/png" }),
      });
    });
  } else if (currentPptPlaybackSource) {
    for (const file of currentPptPlaybackSource.files || []) {
      const relativePath = file.webkitRelativePath || file.name;
      if (!window.PptxClippings.isImageZipEntry(relativePath)) continue;
      files.push({
        fileName: relativePath,
        blob: file,
      });
    }
  } else {
    currentExcelPlaybackImages.forEach((image) => {
      files.push({
        fileName: getBaseFileName(image.name),
        blob: new Blob([image.data], { type: image.mime || "image/png" }),
      });
    });
  }
  if (!files.length) return "";
  return window.PlaybackSourceCache.putPlaybackSource({
    name: currentPptPlaybackSource ? currentPptPlaybackSource.name : "Excel 内嵌后台播放数据截图",
    files,
  });
}

function shouldPersistPlaybackSourceForAutoPpt(options) {
  return Boolean(
    options.autoGeneratePpt
    && isReleaseInfoLikeMode(options.autoPptMode || elements.pptModeInput.value)
    && (currentPptPlaybackSource || currentExcelPlaybackImages.length)
  );
}
```

- [ ] **Step 2: buildPptByMode 接入 playbackImages**

把 `buildPptByMode`（第 935-960 行）开头几行：

```js
async function buildPptByMode(source, modeValue, options = {}) {
  const matchingTasks = options.tasks || (allParsedTasks.length ? allParsedTasks : parsedTasks);
  const templateBytes = options.templateBytes || (currentPptTemplate && currentPptTemplate.bytes) || undefined;
  const fanImages = await resolveFanImagesForPpt(options);
  const pptOptions = { templateBytes, fanImages };
```

改为：

```js
async function buildPptByMode(source, modeValue, options = {}) {
  const matchingTasks = options.tasks || (allParsedTasks.length ? allParsedTasks : parsedTasks);
  const templateBytes = options.templateBytes || (currentPptTemplate && currentPptTemplate.bytes) || undefined;
  const fanImages = await resolveFanImagesForPpt(options);
  const playbackImages = await resolvePlaybackImagesForPpt(options);
  const pptOptions = { templateBytes, fanImages, playbackImages };
```

（函数其余部分不变：`release-info-screenshot`/`dawanqu` 分支本就 `...pptOptions` 展开，`playbackImages` 自动随之传入；`link-screenshot`/默认分支本就只传 `{templateBytes}`，不受影响。）

- [ ] **Step 3: buildPptResultInfo 与 buildPptSourceInfo 文案**

把 `buildPptResultInfo`（第 1046-1058 行）整体替换为：

```js
function buildPptResultInfo(mode, result) {
  const fanText = result.fanMatchedCount ? `，其中 ${result.fanMatchedCount} 页含粉丝量截图` : "";
  const playbackText = result.playbackMatchedCount ? `，${result.playbackMatchedCount} 页含后台播放数据截图` : "";
  if (mode.value === "link-screenshot") {
    return `已生成：${result.imageCount} 张图片，${result.slideCount} 页链接截图单图单页`;
  }
  if (mode.value === "release-info-screenshot") {
    return `已生成：${result.imageCount} 张图片，${result.slideCount} 页发布信息截图单图单页${fanText}${playbackText}`;
  }
  if (mode.value === "dawanqu") {
    return `已生成：${result.imageCount} 张图片，${result.slideCount} 页大湾区崭新模版${fanText}${playbackText}`;
  }
  return `已生成：${result.imageCount} 张图片，${result.slideCount} 页发布剪报，每页 ${result.imagesPerSlide} 张，布局 ${result.grid}`;
}
```

把 `buildPptSourceInfo`（第 1037-1044 行）整体替换为：

```js
function buildPptSourceInfo(name, imageCount) {
  const mode = getCurrentPptMode();
  const matchingTaskCount = allParsedTasks.length || parsedTasks.length;
  const taskText = (mode.value === "link-screenshot" || isReleaseInfoLikeMode(mode.value)) && matchingTaskCount ? `，将优先匹配 ${matchingTaskCount} 条已导入任务` : "";
  const fanCount = currentPptFanSource ? currentPptFanSource.imageCount : currentExcelFanImages.length;
  const fanText = isReleaseInfoLikeMode(mode.value) && fanCount ? `，粉丝量截图 ${fanCount} 张将按 Excel 序号+昵称匹配` : "";
  const playbackCount = currentPptPlaybackSource ? currentPptPlaybackSource.imageCount : currentExcelPlaybackImages.length;
  const playbackText = isReleaseInfoLikeMode(mode.value) && playbackCount ? `，后台播放数据截图 ${playbackCount} 张将按 Excel 序号+昵称匹配` : "";
  return `${name}，识别到 ${imageCount} 张图片，将生成${mode.label} PPT${taskText}${fanText}${playbackText}`;
}
```

- [ ] **Step 4: startRun 接入持久化**

在 `startRun` 的 `options` 对象定义里，紧邻 `autoPptFanSourceId: "",`（第 3322 行）之后插入：

```js
    autoPptPlaybackSourceId: "",
```

紧邻 `shouldPersistFanSourceForAutoPpt(options)` 的整个 `if` 块（第 3331-3348 行）之后插入：

```js
  if (shouldPersistPlaybackSourceForAutoPpt(options)) {
    try {
      isPreparingSupplementStart = true;
      updateStartButtonDisabled();
      addLog("正在缓存后台播放数据截图来源，供截图完成后自动 PPT 使用...");
      options.autoPptPlaybackSourceId = await persistPlaybackSourceForAutoPpt();
      const playbackCount = currentPptPlaybackSource ? currentPptPlaybackSource.imageCount : currentExcelPlaybackImages.length;
      const playbackOrigin = currentPptPlaybackSource ? "" : "（来自 Excel 内嵌图片自动识别）";
      addLog(`后台播放数据截图来源已缓存：${playbackCount} 张${playbackOrigin}，将在自动 PPT 中按 Excel 序号+昵称匹配`, "success");
    } catch (error) {
      // 后台播放数据截图只是自动 PPT 的锦上添花项，缓存失败不应该阻断本次与它无关的整批截图任务。
      addLog(`后台播放数据截图缓存失败，本次截图任务仍会继续，自动生成的 PPT 将不含后台播放数据截图：${error.message}`, "warning");
      options.autoPptPlaybackSourceId = "";
    }
    isPreparingSupplementStart = false;
    updateStartButtonDisabled();
  }
```

- [ ] **Step 5: autoGeneratePptFromCompletedRun 接入**

在 `autoGeneratePptFromCompletedRun`（第 3485-3527 行）里，把：

```js
    const result = await buildPptByMode(source, mode.value, {
      tasks,
      title: state.options.autoPptTitle || "",
      templateBytes,
      autoPptFanSourceId: state.options.autoPptFanSourceId || "",
      modeValue: mode.value,
    });
```

改为：

```js
    const result = await buildPptByMode(source, mode.value, {
      tasks,
      title: state.options.autoPptTitle || "",
      templateBytes,
      autoPptFanSourceId: state.options.autoPptFanSourceId || "",
      autoPptPlaybackSourceId: state.options.autoPptPlaybackSourceId || "",
      modeValue: mode.value,
    });
```

- [ ] **Step 6: 运行回归测试 + 浏览器手动验证**

Run: `node tools/verify_extension_logic.js`
Expected: 全部测试通过（本任务未改动任何被测函数的行为）。

Run: 重新加载插件，切到"发布信息截图单图单页"模式，分别上传一个仅含粉丝量截图的 ZIP 和一个仅含后台播放数据截图的 ZIP（文件名均为 `1_测试.png` 之类），导入一份包含对应任务的 Excel，点击"生成并下载 PPT"。
Expected: 状态文案里能看到"其中 1 页含粉丝量截图，1 页含后台播放数据截图"字样（若任务能精确匹配），下载动作被触发且无控制台报错。

- [ ] **Step 7: 提交**

```bash
git add popup.js
git commit -m "feat: wire playback screenshot resolution and persistence into PPT generation flow"
```

---

### Task 13: auto-ppt.js 接入 playbackImages

**Files:**
- Modify: `auto-ppt.js`

**Interfaces:**
- Consumes: `window.PlaybackSourceCache`（Task 1）、`window.PptxClippings.loadImagesFromCacheRecord`（Task 6，注意本文件第 52 行已在 Task 6 里改名）、`state.options.autoPptPlaybackSourceId`（Task 7）。

- [ ] **Step 1: 新增 resolvePlaybackImagesForPpt**

在 `resolveFanImagesForPpt` 函数（第 46-57 行）之后插入：

```js
async function resolvePlaybackImagesForPpt(options = {}) {
  if (Array.isArray(options.playbackImages)) return options.playbackImages;
  const id = options.autoPptPlaybackSourceId;
  if (!id || !window.PlaybackSourceCache) return [];
  try {
    const record = await window.PlaybackSourceCache.getPlaybackSource(id);
    if (record) return await window.PptxClippings.loadImagesFromCacheRecord(record);
  } catch (error) {
    setStatus(`读取后台播放数据截图缓存失败：${error.message}`);
  }
  return [];
}
```

- [ ] **Step 2: buildPptByMode 接入**

把 `buildPptByMode`（第 59-74 行）开头几行：

```js
async function buildPptByMode(source, modeValue, options = {}) {
  const matchingTasks = options.tasks || [];
  const templateBytes = options.templateBytes || undefined;
  const fanImages = await resolveFanImagesForPpt(options);
  const pptOptions = { templateBytes, fanImages };
```

改为：

```js
async function buildPptByMode(source, modeValue, options = {}) {
  const matchingTasks = options.tasks || [];
  const templateBytes = options.templateBytes || undefined;
  const fanImages = await resolveFanImagesForPpt(options);
  const playbackImages = await resolvePlaybackImagesForPpt(options);
  const pptOptions = { templateBytes, fanImages, playbackImages };
```

- [ ] **Step 3: runAutoPptGeneration 传递 autoPptPlaybackSourceId**

把 `runAutoPptGeneration`（第 93-129 行）里的：

```js
    const result = await buildPptByMode(source, modeValue, {
      tasks,
      title: state.options.autoPptTitle || "",
      templateBytes,
      autoPptFanSourceId: state.options.autoPptFanSourceId || "",
    });
```

改为：

```js
    const result = await buildPptByMode(source, modeValue, {
      tasks,
      title: state.options.autoPptTitle || "",
      templateBytes,
      autoPptFanSourceId: state.options.autoPptFanSourceId || "",
      autoPptPlaybackSourceId: state.options.autoPptPlaybackSourceId || "",
    });
```

- [ ] **Step 4: 提交**

```bash
git add auto-ppt.js
git commit -m "feat: wire playback screenshot resolution into auto-ppt generation page"
```

---

### Task 14: 全量回归 + 人工端到端验证

**Files:** 无代码改动，仅验证。

- [ ] **Step 1: 全量自动化回归**

Run: `node tools/verify_extension_logic.js`
Expected: `All N extension logic regression tests passed.`，`N` 应比改动前多 6（`testPlaybackSourceCacheRoundTrip`/`testAuxImageMatching`/`testLayoutAuxImages`/`testReleaseInfoItemsResolveFanAndPlayback`/`testAutoPptPlaybackSourceIdFlowsThroughOptions`，以及原有测试数不变）。

- [ ] **Step 2: 人工端到端验证四种辅助图组合（覆盖 Task 5 无自动化测试的 XML 渲染层）**

在 Chrome 里重新加载插件，准备一份含 3-4 行任务的 Excel（或直接用手动上传），构造覆盖以下组合的测试数据，模式选"发布信息截图单图单页"（内置模板路径）：
1. 任务 A：无粉丝图、无播放图
2. 任务 B：仅粉丝图（沿用旧有测试素材）
3. 任务 C：仅播放图
4. 任务 D：粉丝图 + 播放图都有

生成 PPT 后用 PowerPoint/WPS/Keynote 打开，逐页检查：
- 任务 A 页：主图铺满整个图框
- 任务 B 页：主图占左半，粉丝图铺满右半（与改动前效果一致）
- 任务 C 页：主图占左半，播放数据图铺满右半（**重点核对**：不应显示"粉丝量截图"占位或空白，也不应残留粉丝图内容）
- 任务 D 页：主图占左半，右半上下两张图（上粉丝、下播放）

- [ ] **Step 3: 重复 Step 2，模式切到"大湾区崭新模版"（preserveTemplateLayout 路径）**

Expected: 同样四种组合都符合设计文档 4.3 节描述的行为（`fanPlaceholder` 存在时用其自身几何框、不存在时对 `mainBox` 做左右二分）。

- [ ] **Step 4: 验证弹窗关闭后的自动 PPT 路径**

勾选"截图完成后同时生成当前 PPT 模式"，上传粉丝图+播放图来源后启动一个小批量截图任务，截图过程中关闭弹窗（触发 `auto-ppt.html` 独立页面接管）。
Expected: `auto-ppt.html` 页面自动打开、生成并下载 PPT，其中辅助图渲染效果与 Step 2/3 一致，说明 `autoPptPlaybackSourceId` 经 `chrome.storage.local` session 透传链路（Task 7）工作正常。

- [ ] **Step 5: 记录结果**

若发现任何组合渲染错误，回到 Task 5 对照设计文档 4.3 节的四种组合表格排查（大概率是 `imageLinks.fan`/`imageLinks.playback` 判空逻辑或 `layoutAuxImages` 顺序问题）。全部通过后本次改动完成，无需额外提交（前面每个任务已分别提交）。
